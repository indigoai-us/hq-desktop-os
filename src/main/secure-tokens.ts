/** @deprecated Import from `./credential-store.js` (US-010). */
export {
  CredentialStore,
  SecureTokenStore,
  cliTokenCachePath,
  CLI_TOKEN_CACHE_SEGMENTS,
  secureStorageAvailable,
} from './credential-store.js';
